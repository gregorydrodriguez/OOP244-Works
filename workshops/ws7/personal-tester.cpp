#include <iostream>

#include "MotorVehicle.h"
#include "MotorVehicle.h"  // intentional
#include "Truck.h"
#include "Truck.h"  // intentional

using namespace std;
using namespace sdds;

int main() {
    // MotorVehicle mv1 = MotorVehicle("VVV-111", 2010);
    // mv1.moveTo("Downtown Toronto");
    // mv1.write(cout);
    // mv1.read(cin);
    // mv1.write(cout);
    // cin >> mv1;
    // cout << mv1;

    // Truck t1 = Truck("VVV-111", 2010, 10.0, "Factory");
    // t1.addCargo(5.0);
    // t1.write(cout);
    // t1.unloadCargo();
    // t1.write(cout);
    // t1.read(cin);
    // t1.write(cout);
    // cin >> t1;
    // cout << t1;
}